import type { Worksheet } from '@/types';

// Empty on first run — worksheets are added via the Admin area once you upload
// question sheets, answers, images or corrections.
export const worksheetsSeed: Worksheet[] = [];
